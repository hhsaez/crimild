SET( CRIMILD_INCLUDE_DIRECTORIES 
	${CRIMILD_SOURCE_DIR}/core/src )

SET( CRIMILD_LIBRARY_LINK_LIBRARIES 
	crimild_core )

INCLUDE( FindLibXml2 )
IF ( LIBXML2_FOUND )
	SET ( CRIMILD_LIBRARY_LINK_LIBRARIES ${CRIMILD_LIBRARY_LINK_LIBRARIES} ${LIBXML2_LIBRARIES} )
	SET ( CRIMILD_INCLUDE_DIRECTORIES ${CRIMILD_INCLUDE_DIRECTORIES} ${LIBXML2_INCLUDE_DIR} )
ELSE ( LIBXML2_FOUND )
	MESSAGE ( ERROR "LibXML2 Not found" )
	RETURN()
ENDIF ( LIBXML2_FOUND )

INCLUDE( ModuleBuildLibrary )

